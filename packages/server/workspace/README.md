# Data Dev IDE Workspace

This is the workspace root. Create SQL files and projects here.

## Getting Started

- Open a `.sql` file to start editing
- Use `Cmd+S` to save
- Use `Shift+Alt+F` to format SQL
